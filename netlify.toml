[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback — must come AFTER function routes, so /api/* is never caught here
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
